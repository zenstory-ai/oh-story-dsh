# workflow-setup.md：开书工作流（Phase 1-3）

本文件用于规划/开书/补纲。先按 SKILL.md「写作流程」确定交付范围；完整读取不等于执行所有 Phase。只讨论时取所需技法，不执行建档步骤；其余按范围补必要前置，不重做已有设定。

---

### Phase 1：确认选题方向

**先查选题决策**：如果项目根存在 `选题决策.md`（story-long-scan Phase 5 产出），读取它——取排在最前（可行性最高）的推荐选题作为开书起点，向用户确认：「扫榜建议写 X（能爆的原因 Y，差异化 Z），按这个开书？」并看 `扫榜日期`：距今较久则提示"市场数据可能过期，建议复扫"。用户认可 → 带该选题的题材/卖点/差异化进入 Phase 2。
缺失时先自动找一遍：从项目根及其上一级目录起、向下最多 3 层按文件名搜 `选题决策.md`（跳过隐藏目录），按 mtime 由新到旧取最新 2 份。搜到 → 用 AskUserQuestion 列候选（路径 + 文件里的 `扫榜日期`，另加「我自己给路径」和「都不用」）；选中某份 → 复制到项目根，再按上一段读取；选「我自己给路径」→ 让用户粘路径，同样复制到项目根；选「都不用」→ 走下面的常规提问。搜不到 → 问一句：「有扫榜生成的 `选题决策.md` 吗？放到项目根或粘贴路径；没有就直接答下面的问题。」仍无 → 走下面的常规提问。

如果用户已有方向 → 跳过下面的方向提问，但仍先做下方「对标上下文加载」（含对标发现），再进入 Phase 2。

如果用户没有方向：

问用户：**「你想让读者什么感觉？有没有喜欢的书想对标？你的优势是什么（脑洞好/文笔好/节奏感好/生活经验丰富）？」**

#### 对标上下文加载

> **拆文库/对标关系** 与 **对标书路径查找** 的定义见 SKILL.md「路径与术语约定」，本文件与 Phase 4 共用同一条规则。

**对标发现（先于下方反应式加载）**：无论用户是否点名对标书，先扫 `拆文库/` 按题材主动推荐，不要被动等用户开口。

1. `ls 拆文库/`（数据源）与项目 `对标/`（引用视图）列已有书目；先按当前项目目录名、`.active-book` 和 `设定/题材定位.md` 中的本书信息识别当前作品，排除同名或来源指向当前正文的 `拆文库/{当前书}/`。story-import 为重建工程生成的本书拆文分析不是对标候选，也不得复制回 `对标/`。排除后都为空 → 跳到第 4 条。
2. 逐本读题材（短篇读 `拆文库/{书}/_meta.json` 的 `genre_detected`；长篇读 `概要.md` 头部或 `拆文报告.md`「基本信息」的「题材」），与本书题材/方向比对，标 同题材 / 弱相关 / 不相关。
3. 有同题材或弱相关候选 → 用 AskUserQuestion 推荐（列候选书 +「都不用」+「另拆一本」），主对标 1 本走文风/正文、其余作副对标。选定后写入 `设定/题材定位.md`「对标登记」的 `主对标书` / `对标书列表`（Phase 2 落文件，此处先记选择），并按 SKILL.md「路径与术语约定」的「首次引用对标书」规则把选中书从 `拆文库/{书}/` 复制到 `对标/{书}/`。
4. 无候选 → 无对标继续（大纲按八节点自排，见 Phase 3）；用户想对标可先 `/story-long-analyze` 拆一本。

如果用户提到对标书或工作目录下已存在 `对标/` 目录：

1. 先按上方「对标发现」第 1 条的同一口径识别当前作品，排除同名或来源指向当前正文的 `对标/{当前书}/`——老导入项目里可能留着历史误建的自对标目录。排除后没有外部对标书时不进入下面几步，按无对标继续。
2. 按 [project-files.md](project-files.md) 的缺失处理识别资料代际，再检查 `剧情/情绪模块.md` 与 `剧情/节奏.md`。
3. `剧情/情绪模块.md` 或 `剧情/节奏.md` 缺失时停止并返回 `missing_primary_contract` + `repair_action`，对作者只说「对标书《X》还缺情绪/节奏分析，建议先用 /story-long-analyze 补拆一次」；不得以摘要或报告伪造。两份老权威产物都存在时正常召回，新增字段缺失不阻断。
4. 两个主产物都存在时按权威顺序读取；`拆文报告.md` 只作概览。
5. 如果角色、普通剧情单元或设定子目录存在，写作时按需召回相关模块。

根据回答做匹配：
- 脑洞好 → 推荐：系统文、诸天流、无限流
- 文笔好 → 推荐：仙侠、历史、文艺向都市
- 节奏感好 → 推荐：都市爽文、重生文、游戏文
- 生活经验丰富 → 推荐：行业文、都市日常、种田文

#### Agent 调用：story-architect

story-architect 属于高层级结构设计 agent。轻量题材定位优先由主会话完成；只有涉及复杂世界观、多线结构、强反转工程或用户明确要求时，才调用 story-architect。确认选题方向后，如果项目已部署 story-architect agent（检查 `.claude/agents/story-architect.md` 是否存在），可 spawn `Agent(subagent_type: "story-architect", prompt: "项目目录：{dir}\n任务类型：题材定位\n查询参数：{用户选择的方向+对标信息}")` 辅助题材分析和核心梗设计。如 agent 不可用，由主线程直接执行。

> **公共灵感召回（可选）**：有 `灵感库/` 时按 `references/cross-book-recall.md`「公共灵感标签召回」以「适用阶段=设定」查 Top 8 active CBA，辅助核心梗与金手指选型；零命中不阻塞。

> **story-architect 契约摘要（spawn 时必须原样附带）**：部署的 story-architect agent 不认识本 skill 的 `references/reader-contract-and-progression.md`，只能靠 spawn prompt 里带的这段摘要对齐 schema，否则主线程和委托产出会用不同的推进规则。摘要内容：
> - **终局储备边界**：终局底牌（头号宿敌/终极真相/金手指上限/身份终点/核心情感终点）是一次性资源，逐卷解锁，不得提前打光；升级台阶（境界/等级/地图/势力层级）按剩余档数逐级解锁，不得越级。
> - **剧情单元**：卷纲内写剧情单元卡，含单元ID、主推线（1条）+ 战果线（若干，一战多得允许）、章级推进下限（快节奏保留可见事件/爽点下限）。
> - **细纲层字段**：每章细纲必须带 单元ID/位置、主角目标/关键选择；「行动成本（可无）/收益归属」替代旧字段「代价兑现」——行动成本可无，不硬造代价，收益归属必须可见。
> - **契约四问**：①主角是不是靠自己的选择挣到结果（不要求事事亲自动手，但决定事情为什么发生、作出关键选择、拿到收益的是主角或本卷已承诺的所有者）？②核心收益或高光有没有被配角、机构或巧合无交换地拿走？③有没有动用本阶段不该解锁的终局底牌、或把升级线顶到天花板？④旧期待付了利息没，又留下什么期待？四问都过＝契约安全，能补＝需补强，补不上＝契约破坏。
> - 完整规则以主会话已加载的 `references/reader-contract-and-progression.md` 为准，此处只是给 story-architect 的压缩版；agent 本身不部署该文件，不能自己读取。

---

### Phase 2：核心设定

从 Phase 1 确定的目标情绪出发，在题材框架中找到对应的剧情模式，从对标书提取可复用模块（把具体角色看成功能位），用用户自己的角色和设定填充。

帮用户确立以下核心要素：

```
## 核心设定表

### 基本信息
- 书名：{暂定名}
- 题材/类型：{主类型 + 副类型}
- 目标平台：{起点/番茄/晋江/其他}
- 预计字数：{X} 万字
- 目标读者：{画像}
- 读者契约：{本书承诺的核心阅读快感；参 reader-contract-and-progression.md}
- 主角高光/代理权：{主角不可替代的判断、选择或贡献}
- 核心期待债：{开篇必须偿还的承诺}

### 一句话梗概
{主角 + 目标 + 阻碍 + 反转，一句话概括全书}

### 主角设定
- 姓名：{}
- 年龄：{}
- 核心特质：{2-3 个关键词}
- 金手指/核心能力：{}
- 弱点/缺陷：{让角色更立体的地方}
- 核心动机：{他为什么要做这件事}

### 世界观骨架
- 时代/背景：{}
- 核心设定：{区别于同类作品的独特设定}
- 力量体系：{如果有，简单概括}
- 社会结构：{影响故事的关键设定}

### 核心冲突
- 主线矛盾：{}
- 终极 Boss/终极阻碍：{}
```

完整开书时创建以下 artifact；仅规划时只落与所请求结构有关的已定设定，不强制补全角色档案或题材正文提示卡（模板见 [references/artifact-protocols.md](artifact-protocols.md)）：
- **设定/关系.md**：角色关系映射（参考 character-relations.md「四种关系类型」）
- **设定/题材定位.md**：题材核心梗三分法+对标分析（参考 long-genre-mechanics.md「核心梗解析」）。对标分析表保留 2-3 行摘要，详细数据见 `对标/` 目录
- **设定/题材正文提示卡.md**：从 `设定/题材定位.md` + `references/genre-prose-cards.md`（索引）+ `references/genre-prose-cards/`（单题材正文卡目录，按题材分类优先）+ `references/style-genre-modules.md`（通用流派补充）抽取本书正文层题材卡，只写题材边界、核心逻辑、读者期待、核心爽点/情绪、正文落点、前中后期打法、节奏密度、场景颗粒、禁止漂移；不写通用格式规则，不覆盖 `设定/文风.md`

> **多对标书时**：参 `references/cross-book-recall.md`，副对标 anchor 入「对标分析」表附录

#### Agent 调用：story-architect + character-designer

核心设定阶段，当前端已部署对应 agent 时可 spawn；Antigravity 按 SKILL.md 用 `invoke_subagent` + 同名 `TypeName`：
- `Agent(subagent_type: "story-architect", prompt: "项目目录：{dir}\n任务类型：核心设定\n查询参数：世界观构建+核心冲突设计")` — 辅助世界观和核心冲突设计；spawn prompt 必须原样附带 Phase 1 的「story-architect 契约摘要」（升级台阶检查约束力量体系设计）
- `Agent(subagent_type: "character-designer", prompt: "项目目录：{dir}\n任务类型：角色设定\n查询参数：{主角设定信息}")` — 辅助角色设定和语言风格档案

如 agent 不可用，由主线程直接执行。

---

### Phase 3：大纲搭建

#### 全书体量与阶段总览（卷纲前置）

卷级大纲前先写全书体量与阶段边界，作为后续卷纲、细纲、日更补纲的共同约束。比例只作默认参考，必须按题材、目标字数和对标节奏调整，不能机械套模板。

```
## 全书体量与阶段总览

- 全书总章节数：{X} 章
- 目标字数：{X} 万字
- 全书情绪曲线：{压抑/期待 → 加压/反转 → 爽感/震撼 → 余韵/圆满；标注关键阶段}

### 阶段划分
1. 开篇期（第 {A}-{B} 章，参考 10%-15%）：核心任务 {立人设/立世界/埋主线钩子}；情绪基调 {压抑/期待/惊奇}；阶段结束时读者应建立 {对主角的认同 + 对主线冲突的期待}
2. 发展期（第 {A}-{B} 章，参考 50%-60%）：核心任务 {矛盾展开/支线并行/资源与关系升级}；情绪基调 {期待/紧张/间歇爽感}；阶段结束时读者应建立 {对核心反派或终局矛盾的迫切期待}
3. 高潮期（第 {A}-{B} 章，参考 20%-25%）：核心任务 {核心冲突爆发/多线收束/连续爽点}；情绪基调 {爽感/震撼/反转}；阶段结束时核心矛盾应 {基本解决或进入最终对决}
4. 收尾期（第 {A}-{B} 章，参考 5%-10%）：核心任务 {伏笔回收/关系落定/结局余韵}；情绪基调 {释然/感动/圆满}；阶段结束时主线伏笔应 {回收完毕，结局落定}

### 各阶段节奏公式
- 开篇期：核心任务 / 情绪基调 / 爽点密度 / 伏笔策略 / 冲突层级 / 阶段钩子 / 禁止提前释放
- 发展期：核心任务 / 情绪基调 / 爽点密度 / 伏笔策略 / 冲突层级 / 阶段钩子 / 禁止提前释放
- 高潮期：核心任务 / 情绪基调 / 爽点密度 / 伏笔策略 / 冲突层级 / 阶段钩子 / 禁止提前释放
- 收尾期：核心任务 / 情绪基调 / 爽点密度 / 伏笔策略 / 冲突层级 / 阶段钩子 / 必须回收清单

### 关键节点与钩子链
- 关键转折点：第 {X/Y/Z} 章，各自承担 {事件功能 + 情绪效果}
- 小/中/大高潮：小高潮第 {X/Y/Z} 章；中高潮第 {X/Y} 章；大高潮第 {X} 章
- 阶段钩子链：开篇期结尾指向发展期；发展期结尾指向高潮期；高潮期结尾指向收尾期
```

#### 卷级大纲（全书结构）

```
## 卷级大纲

### 第一卷：{卷名}（约 {X} 万字，{Y} 章）
- 功能：{铺垫/起步/第一个大爽点}
- 所属阶段：{开篇期/发展期/高潮期/收尾期；可跨阶段时写清分界章}
- 卷契约：{本卷承诺的读者快感、主角高光、主要期待债}
- 终局储备：{本卷主推线1条 + 战果线若干（一战多得允许）；本卷解锁哪个终局里程碑、禁碰哪些未解锁底牌，参 `设定/题材定位.md`「终局底牌与升级台阶」小节 与 reader-contract-and-progression.md}
- 剧情单元：{1–3 万字剧情单元卡写在卷纲内，不另建单独文件；字段模板见 `references/artifact-protocols.md`，契约/推进规则见 `references/reader-contract-and-progression.md`}
- 阶段边界：本卷可释放 {信息/关系/能力}；本卷禁止提前释放 {后期核心真相/终局底牌}
- 核心事件：{一句话}
- 起始状态 → 结束状态：{主角从 {A} 变成 {B}}

### 第二卷：{卷名}
...

### 最终卷：{卷名}
- 功能：{高潮 + 收尾}
- 核心事件：{一句话}
```

> **多对标书时**：参 `references/cross-book-recall.md`，副对标 `章节/*_摘要.md` + `剧情/*.md` 召回卷级节奏

> **对标节奏回流（有主对标书时，卷纲定稿前做一次）**：读主对标 `对标/{书}/剧情/节奏.md`；资料缺失时设置 `missing_primary_contract` 并停止，不得以摘要、故事线或报告代替。文件存在时，按 references/outline-structure-theory.md「对标节奏迁移」将 1/4·中点·3/4 换素材排入卷纲「对标结构坐标」，选中单元写入「对标剧情参照」（也可填「自创：{理由}」），并过排纲自查。未登记主对标但有同题材候选时先回 Phase 1；确无对标则按八节点自排。有 `灵感库/` 时可另按「适用阶段=卷纲」召回 Top 8 active CBA 辅助结构选型，零命中不阻塞。

#### 排纲自查与细纲

**排纲自查**（每卷、每批细纲设计前答一遍；逐章不重答）：
1. **情绪引擎**：本卷交付什么情绪、靠什么剧情模式可靠交付、核心冲突是什么？先按 `references/emotional-methods.md` 设计正向发动机，再做两个删除检验——删掉题材核心的对象/关系/问题，或删掉主角，情绪兑现还基本成立吗？成立就说明引擎没立住，先修。
2. **节奏与定位**：哪段加速哪段减速；章节定位有高低、不是全程高压，低压加过场合计不超约 15%；有对标书时 1/4、中点、3/4 各锚一个关键情节（中途追加单元按单元内的 1/4·中点·3/4，不改已锁定的卷尺度坐标表；见 references/outline-structure-theory.md「章节定位与张弛 / 对标节奏迁移」）。
3. **伏笔与阶段边界**：本卷新埋哪些伏笔、上一卷待收的怎么处理；本卷在全书哪个阶段，可释放什么、必须压住什么（压住的写进细纲「本章禁止提前释放」）。
4. **契约四问**：按 `references/reader-contract-and-progression.md` 逐剧情单元过一遍，把 契约安全 / 需补强 / 契约破坏 写进单元卡「风险等级」；契约破坏 先修纲，需补强 补交换、铺垫或成本。不要求主角亲自完成每个动作。
5. **供给**：进入细纲层时按下方批次步骤 3 做供给自查。

只交付大纲/卷纲时，完成排纲自查 1–4 和本节末对应纲文件后停止，不执行下方细纲与设定补全。进入细纲层后，所请求范围内每章必须有 `大纲/细纲_第XXX章.md`，不跳章。

细纲按剧情批构建（见 references/outline-structure-theory.md「按剧情批出细纲」）；仅未限定范围的完整开书默认前 10 章，其他按 SKILL.md 的范围与停点交付：
1. **批次边界**：未指定章数时，一批 ≈ 一个剧情单元（5-15 章）；>10 章拆两批。开书首批前 10 章封顶、可跨单元；每单元建卡时召回一次，后续批共享结论。用户指定章数/范围时，只建范围内细纲，不因单元未完或剩余 ≤2 章而扩批；单批仍建议 ≤10 章。日更缺纲按 workflow-daily 的补建流程处理。
2. **批次定位与阶段约束**（每批补纲前先写）：当前章节区间属于哪个阶段与哪个剧情单元（单元ID）、本批推进目标、本批可释放信息、本批严禁提前释放信息、章尾钩子不能越过的边界。
3. **供给自查**（排纲自查第 5 项）：每批出细纲前检查六项：①对手戏由谁承接？具名对手间隔超过 2 章是复核提示；对手可为盟友或亲人，环境、关系章不为凑间隔新增反派。②未来 3 章出场角色的设定卡是否齐全？③能力增量与既有库存是否接得上？④本批引用的数字、制度、规矩、距离与脚程是否确实存在？引用前 grep 核对文件里的具体条目，不能只猜文件存在。⑤本批使用的地点空间属性先落档；仅提名不强补底图。是否与上一单元同场景、同对手、同胜负手连排，需要调整？⑥本批有哪些故事线推进，是否有可调存量？结果以「供给自查」小节写进 `大纲/排纲底稿_{单元ID}.md`（不写进卷纲）：「写了要报」登记新增内容与设定路径，「先问作者」列候选及影响面交作者裁定；无缺口写「无缺口」。并检查上一批「本章没写成的」与「建纲追加」里的未解决缺口。
4. **剧情单元消费**：建卡批读本批剧情单元卡「对标剧情参照」指向的剧情单元（≤3 个文件，含结构分布/情节点索引），按节拍→章映射铺章、情节点换素材；同一剧情单元的后续批共享剧情单元卡固化结论，不重读剧情单元。当前卷纲缺少必需剧情单元字段时先补齐卷纲，不带缺失字段进入日更。
5. **建纲当场缺口**：按下方三级处置——直接写的写进细纲，写了要报的先落档再引用，先问作者的挂起受影响内容，其余部分继续。批末把后两级汇入同一份排纲底稿的「建纲追加」小节：章号、缺口、级别、设定路径或提案；无追加写「无」。不重复记录直接写的现场材料。
6. **批末结构复核**（每批建完必做）：按现有大纲流程确认章级目标与 `visible_chars_v1` 口径、核心字段、禁止提前释放和可执行情节点齐全。不得用“每个情节点固定等于多少字”预测容量或自动补事件；每章细纲落盘后必须跑 `node scripts/check-outline-contract.js --json --project {书目录} --chapter {N}`：exit 1 只按 `repair_scope` 补点名字段再复验，最多两轮；advisory 项逐条人工判断，不强制归零。批末另跑一次 `node scripts/check-outline-contract.js --json --supply {卷纲路径} {单元ID}` 验证本批供给自查已写进排纲底稿（含「无缺口」情形）；脚本仅验供给小节存在，内容仍需人工判断。**旧章细纲缺字段是常态，不要照抄同目录既有文件的字段集**，以本节模板为准。语义仍不设 checker。

不强行一次产出 30 章细纲；全书 ≤30 章且用户明确要全书细纲时，可分批连续交付。

```
## 细纲（第 N 章）

### 第 N 章：{章名}
- 核心事件：{一句话；供日更与导入统一消费}
- 字数目标：{X} 字
- 字数口径：visible_chars_v1
- 单元ID/位置：{卷纲剧情单元ID；单元内第几拍/承担功能}
- 目标情绪：{具体情绪前状态→后状态；本章推进单元情绪引擎的哪一环；不得只写“热血/悲伤”等标签}
- 主角目标/关键选择：{主角本章要什么；必须做出的判断或选择}
- 章首钩子：{从章首7式中选择} — {具体内容；低压/过场章可写“弱钩子/仅情绪钩子，功能是…”}
- 爽点：{本章爽点；低压/修炼/过场章可写“无显性爽点，功能是…”，但仍须给读者一个往下看的理由}
- 本章禁止提前释放：{**只写本章特有的三五条**；卷级常任禁忌已在卷纲「本卷禁碰的终局底牌」，不逐章复读；没有就写“无”}

#### 内容概括（五段式）
- 起因：{本章事件为什么发生}
- 发展：{冲突如何推进}
- 转折：{信息/关系/局势哪里改变}
- 高潮：{本章情绪或动作峰值}
- 结尾：{本章最后落在谁的什么动作/画面/台词上——写具体落点，不写"尘埃落定""一切结束"式状态判词}

#### 情节安排（多线）
- 主线推进：{本章对主目标的推进}
- 辅线推进：{指向卷纲「本卷故事线」的 L 编号，写“L0X 从哪一步推到哪一步”；本章确无非主线在动时写“无”，不凭空制造。一批细纲不许每章都是“无”}
- 事件线 / 任务线：{外部事件链；有“办事被卡住”时写清卡出什么变化，删掉无损的卡点不进细纲}
- 感情线 / 关系线：{无显性感情线时写“无显性，但关系变化为…”}
- 逻辑线：原因 → 行动 → 结果 → 后果/新问题

#### 人物关系和出场顺序
- 出场顺序：{角色/势力/关键物件按实际出现顺序列出}
- 人物关系变化：{本章前 → 本章后}
- 视角/信息差：{谁知道什么；读者知道什么；主角误判什么。取自 `追踪/时间线/读者已知.md`，不另起一套}

#### 情节细化
| # | 情节点（谁做了什么） | 功能标签 | 执行边界（禁＋放） |
|---|---|---|---|
| 1 | {主角在账单上发现4800元转出} | {信息揭示} | {禁：不揭露幕后者。放：他当场算出这笔钱等于几个月房租，允许写出来} |

- 功能标签＝目的词：铺垫/高潮/爽点/打脸/人物塑造/设定。点数由语义需要决定，不填写逐点字数，不用 `目标字数 / beat 数` 反推。
- **执行边界写「禁＋放」两半**：禁＝本点不能提前释放或新增什么；放＝本点允许什么落地、到什么量级（谁可以出声、谁可以失态、允不允许当场标价）。**每章至少一个点写了「放」**；放行是解除限制，不是义务。
- 复沓锚句：{须一字不差进正文的原话（誓言、面板、旧案原话），一行一条并注明落点，如"点3：立此为凭…"；没有写"无"}
- 行动成本（可无）/收益归属：{可无行动成本；收益归谁、如何可见、是否留下后续账}

#### 结尾设定和钩子
- 结尾设定：{收束落到什么具体动作或画面（不写"就这样……""他终于明白……"式状态判词）；未解决问题；下一章推动力}
- 章尾钩子：{从章尾13式中选择} — {具体内容，期待度：强/中/弱；低压/过场章可弱钩子或留阶段目标；与下一章如何承接}

#### 可选：需要时再写（写了写手照样消费，不写不拦）
- 阶段位置、章节定位（高压/推进/修炼试错/关系回收/低压生活/信息整理）、本章结构公式
- 本章标价（得失折算成读者已知的什么）、闭环状态（获得/使用/见效/估值走到哪一拍）
- 写手自由区（本章最值得放开、由写手发明的几处）、镜头准入（一场给谁台词位）、信息差触发点（在第几点提醒读者手里的牌）
- 情节点表加「分辨率」列（密/中/疏，同类并列递减）
- 契约风险：只在本章让单元卡「风险等级」变化时写
```

**大纲锁定**：已进入正文写作的前 10 章细纲锁定，未经用户确认不得修改；后续滚动细纲可随正文反馈微调。**卷纲锁定的定义**：某卷一旦已有正文章节，该卷已写区间对应的既有剧情单元卡与对标结构坐标即视为锁定——未经用户确认不改既有内容，但允许在卷尾**追加**新剧情单元卡（只增不改）。追加同样只发生在用户明确要求补纲/扩纲时；日更/写正文流程不自动追加剧情单元卡。workflow-daily 各处「锁定卷纲绝不自动修改」均按此定义执行。

**中途补纲/扩纲小流程**（项目已有大纲；只改细纲不进入正文大修）：
1. 定位请求范围、已写进度和所属单元：读 `大纲/大纲.md`；卷纲先用 `outline_view.py --toc {卷纲路径}` 定位，再 `--unit {单元ID}` 取闭包，尚无单元用 `--contract` 取契约，不整读。另读 `设定/题材定位.md` 和已有 `追踪/上下文.md`；有正文却缺追踪时停止补纲，提示按 workflow-daily 修复/迁移，不猜进度；无正文也无追踪时以现有纲/设定规划，不为读取状态先初始化。
2. 请求章已属于既有单元时复用单元卡，只补/改点名细纲，不另建单元；沿用上方锁定规则。确需新单元才按 references/outline-structure-theory.md「对标节奏迁移」选同类单元，向卷尾**追加**卡及情绪弧线、故事线、伏笔规划，已锁定内容不动。用户补纲不等于授权修改其他章；超出本卷体量时先确认扩卷或开新卷。
3. 答上方「排纲自查」1–4（追加单元时按单元内 1/4·中点·3/4 口径）；进入细纲层时再做第 5 项供给自查。
4. 按本次交付层级停止：只要大纲/卷纲则不展开细纲；需要细纲时按上述批次边界只做请求范围，完成「细纲后设定补全」与批末核对。
5. 规划事件和未来揭示只落卷纲/细纲，不提前成为追踪事实。已有正文项目补纲不写 `追踪/`，不执行 init/commit；下一章必须履行的承诺在下一次正文事务里进入 `next_chapter_commitments`。

**细纲质量要求**：每章细纲都要按当前章节蓝图直接指导正文（核心事件、字数目标、字数口径、单元ID/位置、目标情绪、主角目标/关键选择、章首钩子、爽点、本章禁止提前释放九个必填字段齐备），但**强度按章节定位分配、不是每章顶满**：高压/推进章配齐钩子+爽点+悬念；低压/关系/修炼/信息整理章允许无显性爽点、弱钩子或仅情绪钩子，重点是把功能（喘息、关系、铺垫、转场）写到位。底线是每章都给读者一个往下看的理由、相邻章不情绪趋同（见 references/outline-structure-theory.md「章节定位与张弛」）。字段不完整时先补齐；无法从材料确定的关系或副线写 `[待补充]`，不得杜撰。

> **细纲钩子来源**：优先沿本批剧情单元卡「对标剧情参照」剧情单元的节拍与兑现方式设计钩子链；多对标书时参 `references/cross-book-recall.md`，副对标同基调 `章节/*_摘要.md` 仍可作单章钩子灵感（可选）；有 `灵感库/` 时可按「适用阶段=细纲」召回 active CBA 辅助钩子设计，零命中不阻塞

**章节标题规则**：只做轻量去重；发现同名或明显重复标题时，按本章核心事件改名，并保持细纲标题与正文文件名一致。

**细纲后设定补全（每批细纲建完后执行）**：扫描本批细纲新出现的具名角色/势力/关键设定，对**会复用**的（按卷纲/细纲判断：后续多次出场或承担剧情功能）自动建档，不等用户确认：
- 角色 → 建 `设定/角色/{名}.md`（填空模板见 character-basics.md 主角卡/配角卡）；核心复用角色真正进入正文后，由追踪事务建立 `追踪/角色状态/{名}.md` 动态快照；
- 势力/组织 → 建 `设定/势力/{名}.md`（名称、定位、核心目标、关键人物、与主角关系）；
- 影响多章的世界观规则 → 建/补 `设定/世界观/{主题}.md`（规则、适用范围）。
- **刻度与名目** → 建/补 `设定/世界观/{主题}.md` 里的对照表。凡是正文要拿来**比大小、算价钱、报名次、定档位**的东西都算：资质/评级档位、职级与待遇、货币与物价、等级与进度格、名次与席位、赏罚名目、时限与期限。写成**可数的序列**（几档、哪一档在哪、相邻两档差多少），不要只写一个孤立的值。

**缺什么按新增物三级处置——不许硬写，也不许一律停摆。** 与写手写正文同一套三级。 排纲或写正文时需要一样东西（角色、能力、物价、规矩、刻度、场景）而设定里查不到时，先查再分级：

0. **先查**：`设定/世界观/`、`设定/势力/`、`设定/题材定位.md`（升级台阶）里是不是已经有（同一件事在不同文件里可能叫不同名字，换个说法登记的也算）。查不到的按下面三级处置。
1. **直接写**（场景陈设、路人、地名细部、一次性对话、习俗细节）：直接写进细纲，不落档、不上报。
2. **写了要报**（发明＋落档）：既有框架内的具名配角、复用地点的空间属性、能力形态、物价内插、规矩细则、刻度序列，可直接补充，不与既有裁定冲突。先增量落设定，再供细纲引用，并在排纲底稿的供给自查里登记。项目引用用书目录相对路径（如 `设定/角色/张三.md`）；`./`、`../` 相对细纲目录，裸文件名不校验。地点只补本批用到的分区、进出口、相对位置或脚程。补纲授权下的新故事线先在卷纲故事线表分配 L 编号并写清主线对接口，再供细纲引用；日更不自动扩纲。
3. **先问作者**（提案裁定）：牵连全书的金手指规则、真相、伏笔、主线、力量档位、经济锚点、读者契约及马甲归因，不自行决定。在排纲底稿的供给自查里列具体缺口、1-2 个候选及关联设定影响，批末按下方汇报模板用故事话问作者；裁定先落设定再引用，不以占位或临时编数代替。推翻既有裁定、两个设定文件互相冲突也归这一级，列出出处供作者判断。

已存在的设定文件按细纲新信息**增量补充、不覆盖**。一次性路人、后文无戏份的配角不建静态档案或动态快照。建档只填细纲已确定的信息，未定字段留占位符，不提前杜撰。

按本次交付范围创建以下 artifact（模板见 [references/artifact-protocols.md](artifact-protocols.md)）：
- **大纲/大纲.md**：全书卷级鸟瞰（卷名+字数+章数+核心事件+状态变化，一段式汇总）
- **大纲/卷纲_第X卷.md**：每卷的故事线（L 编号）+剧情单元+情绪弧线（含章节定位）+人物弧线+伏笔+反转+对标结构坐标+**刻度台阶/打脸债/压力线轮转**三表（按需填写，不适用时省略或写「无」；参考 outline-methods.md「大纲三层结构法」 + outline-structure-theory.md「章节定位与张弛 / 对标节奏迁移」 + emotional-arc-design.md「六种弧线速查」 + long-reversal.md「反转类型」；三表模板见 artifact-protocols.md）
- **追踪不在规划阶段初始化**：开书、大纲、细纲、补纲都不建 `追踪/`；首次进入正文时由 workflow-chapter 处理缺 state（无正文初始化第 0 章，事务 JSON 放书目录 `.story/work/`）。核心角色动态快照在首次正文事务中创建。

**规划交付后向作者汇报**（开书、大纲、卷纲、细纲、补纲、改细纲都用；按 SKILL.md「面向作者的汇报」）：排纲自查、结构验收是内部核对，只把结论翻成故事话；「先问作者」的缺口和两个设定互相冲突都作为「要你定的事」问。

<!-- author-report -->
```md
{做了什么，如「第二卷卷纲定好了」「补好了第22-26章细纲」「按你说的改了第23章细纲」}：
- {故事层要点，一行一件：这卷/这几章主要发生什么，主角得到或失去什么，停在哪个悬念上}
- {改细纲时：原来是什么，现在改成什么，会不会牵动别的章}
{要你定的事：一句问题＋白话选项＋推荐，如「沈栀的金手指每天能用几次？1. 一次（推荐，后面好加码）2. 不限次」；没有就删掉此行}
下一步：{如「要接着写第22章正文吗？」「要继续出第27-31章细纲吗？」}
```

前 3 章细纲额外加载 [references/opening-design.md](opening-design.md)（黄金三章法则+六大标准）。

#### Agent 调用：story-architect

规划优先由主会话按请求范围交付；只有结构复杂、反转链多或方案不稳时才调用 story-architect。

当前端已部署 story-architect 时可让它辅助；Antigravity 调用规则同上：
- 任务：传入本次交付层级、章/卷范围与停点；只做卷纲时不让 agent 附带细纲或正文。
- 章节定位（可选）：需要时标高压/推进/修炼试错/关系回收/低压生活/信息整理；低压章可弱爽点，但仍要有往下看的理由。
- 目标字数：只填写章级 `字数目标` 与 `visible_chars_v1` 口径；情节点不分配精确字数，也不让模型心算容量。
- 主会话校验：按既有细纲流程复核目标、核心字段、可执行情节点和 `[待补充]`；本阶段不把语义判断复制进多端 Hook。
- **契约摘要必须原样附带**：本阶段的卷纲要直接产出终局储备（本卷主推线/战果、本卷解锁的终局里程碑、禁碰的未解锁底牌）和剧情单元（单元ID等字段），spawn prompt 必须带上 Phase 1「Agent 调用：story-architect」中的「story-architect 契约摘要」，让主线程与委托产出共用同一 schema。

如 agent 不可用，由主线程直接执行。
